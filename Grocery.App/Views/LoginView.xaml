<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Grocery.App.ViewModels"
             x:Class="Grocery.App.Views.LoginView"
             x:DataType="vm:LoginViewModel"
             Title="Login">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="16" VerticalOptions="Center">
            
            <Label Text="Login" FontSize="22" HorizontalOptions="Center" />
            <Entry Placeholder="Email" Text="{Binding Email}" Keyboard="Email" />
            <Entry Placeholder="Password" Text="{Binding Password}" IsPassword="True" />
            <Button Text="Login" Command="{Binding LoginCommand}" />
            <Label Text="{Binding LoginMessage}" TextColor="Red" />
            
            <BoxView HeightRequest="1" Opacity="0.2" Margin="0,12" />
            
            <Label Text="Register" FontSize="22" HorizontalOptions="Center" />
            <Entry Placeholder="Email" Text="{Binding RegisterEmail}" Keyboard="Email" />
            <Entry Placeholder="Password" Text="{Binding RegisterPassword}" IsPassword="True" />
            <Button Text="Register" Command="{Binding RegisterCommand}" />
            <Label Text="{Binding RegisterMessage}" TextColor="Red" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>