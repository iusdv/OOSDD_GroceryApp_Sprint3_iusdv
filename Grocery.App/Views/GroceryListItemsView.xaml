<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:m="clr-namespace:Grocery.Core.Models;assembly=Grocery.Core"
             xmlns:vm="clr-namespace:Grocery.App.ViewModels"
             x:Class="Grocery.App.Views.GroceryListItemsView"
             x:DataType="vm:GroceryListItemsViewModel"
             Title="GroceryListItemsView">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Wijzig kleur" Command="{Binding ChangeColorCommand}" Priority="0" Order="Primary"
                     IconImageSource="{FontImage Glyph='&#xf30c;', Color=Black, Size=22}" />
        <ToolbarItem Text="Deel boodschappenlijst" Command="{Binding ShareGroceryListCommand}" Priority="1" Order="Primary"
                     IconImageSource="{FontImage Glyph='&#xf30c;', Color=Black, Size=22}" />
    </ContentPage.ToolbarItems>

    <Shell.TitleView>
        <Grid>
            <Label Text="{Binding GroceryList.Name}" FontSize="Large" HorizontalOptions="Center" VerticalOptions="Center"/>
        </Grid>
    </Shell.TitleView>

    <Grid RowDefinitions="Auto,*" ColumnDefinitions="2*,2*">
   
        <Border Grid.Row="1" Grid.Column="0" Stroke="#C49B33" StrokeThickness="4" Padding="10" Margin="10" HorizontalOptions="Center">
            <CollectionView ItemsSource="{Binding MyGroceryListItems}" Margin="5" WidthRequest="300" HorizontalOptions="Start"
                            SelectionMode="None">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="m:GroceryListItem">
                        <Grid ColumnSpacing="10" Padding="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" /> 
                                <ColumnDefinition Width="Auto" /> 
                                <ColumnDefinition Width="Auto" />  
                            </Grid.ColumnDefinitions>

                            <Label Text="{Binding Product.Name}"
                                   Grid.Column="0"
                                   VerticalTextAlignment="Center" />
                            
                            <Label Text="{Binding Amount}"
                                   Grid.Column="1"
                                   VerticalTextAlignment="Center"
                                   Margin="0,0,6,0" />
                            
                            <Button Grid.Column="2"
                                    Text="Verwijder"
                                    FontSize="12"
                                    Padding="8,4"
                                    WidthRequest="70"
                                    HeightRequest="20"
                                    CornerRadius="8"
                                    HorizontalOptions="End"
                                    VerticalOptions="Center"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:GroceryListItemsViewModel}}, Path=RemoveGroceryListItemCommand}"
                                    CommandParameter="{Binding .}" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
                
                <CollectionView.EmptyView>
                    <ContentView>
                        <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center">
                            <Label Text="De boodschappenlijst is nog leeg. Voeg eerst boodschappen toe!" FontAttributes="Bold" HorizontalTextAlignment="Center"/>
                        </VerticalStackLayout>
                    </ContentView>
                </CollectionView.EmptyView>
            </CollectionView>
        </Border>

        <Border Grid.Row="1" Grid.Column="1" Stroke="#C49B33" StrokeThickness="4" Padding="5" Margin="5" HorizontalOptions="Center">
            <StackLayout>
                
                <SearchBar x:Name="ProductSearchBar"
                           Placeholder="Zoek producten"
                           SearchCommand="{Binding SearchProductsCommand}"
                           SearchCommandParameter="{Binding Text, Source={x:Reference ProductSearchBar}}"
                           Margin="5"/>
                
                <CollectionView ItemsSource="{Binding FilteredProducts}" Margin="5" WidthRequest="300" HorizontalOptions="Start"
                                SelectionMode="Single"
                                SelectionChangedCommand="{Binding AddProductCommand}"
                                SelectionChangedCommandParameter="{Binding Source={RelativeSource Self}, Path=SelectedItem}">
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
                    </CollectionView.ItemsLayout>

                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="m:Product">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Label Text="{Binding Name}" Grid.Column="0" />
                                <Label Text="{Binding Stock}" Grid.Column="1" />
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>

                    <CollectionView.EmptyView>
                        <ContentView>
                            <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center">
                                <Label Text="Er zijn geen producten gevonden. Pas je zoekterm aan of voeg nieuwe producten toe!" FontAttributes="Bold" HorizontalTextAlignment="Center"/>
                            </VerticalStackLayout>
                        </ContentView>
                    </CollectionView.EmptyView>
                </CollectionView>
            </StackLayout>
        </Border>

        <Label Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" HorizontalOptions="Center" VerticalOptions="Center"
               Text="{Binding MyMessage}" TextColor="Red" Margin="30,0,0,10"/>
    </Grid>
</ContentPage>
